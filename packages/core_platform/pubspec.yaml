name: core_platform
description: Device and platform integrations for the Flutter starter template.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  core_analytics: ^0.1.0
  core_domain: ^0.1.0
  injectable: ^3.0.0

  camera: ^0.12.0+1
  firebase_crashlytics: ^5.2.2
  firebase_messaging: ^16.2.2
  flutter_local_notifications: ^21.0.0
  image_picker: ^1.2.2
  permission_handler: ^12.0.1
  share_plus: ^13.1.0
  video_player: ^2.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.15.0
  injectable_generator: ^3.0.2
  mocktail: ^1.0.5
