name: test_utils
description: Shared test helpers and mocks for the Flutter starter template.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  analytics: ^0.1.0
  app_platform: ^0.1.0
  storage: ^0.1.0
  mocktail: ^1.0.5
