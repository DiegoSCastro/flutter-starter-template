name: test_utils
description: Shared test helpers and mocks for the Flutter starter template.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  core_analytics: ^0.1.0
  core_platform: ^0.1.0
  core_storage: ^0.1.0
  mocktail: ^1.0.5
