name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel superseded runs on the same ref to save CI minutes.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze-and-test:
    name: Analyze & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      # Pin to the same Flutter SDK as .fvmrc (3.44.0). Bump both together.
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.0
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      # Generated files (*.g.dart, *.freezed.dart, *.config.dart, etc.) are
      # committed to the repo. Regenerate them and fail if anything changed,
      # so a PR can never merge with stale generated code.
      - name: Verify generated code is up to date
        run: |
          dart run build_runner build --delete-conflicting-outputs
          if ! git diff --quiet --exit-code; then
            echo "::error::Generated code is out of date. Run 'dart run build_runner build --delete-conflicting-outputs' and commit the result."
            git diff --stat
            exit 1
          fi

      - name: Verify formatting
        run: dart format --output=none --set-exit-if-changed .

      - name: Analyze
        run: flutter analyze

      # Golden tests are excluded: Flutter goldens are not guaranteed to match
      # across operating systems, and baselines are generated on macOS while
      # this runner is Ubuntu. They run locally as a regression check (see
      # dart_test.yaml). Integration tests under integration_test/ also do not
      # run here — `flutter test` only picks up the test/ directory, and they
      # require a connected device/emulator.
      - name: Run tests
        run: flutter test --coverage --exclude-tags golden

      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage
          path: coverage/lcov.info
          if-no-files-found: ignore

      # Fail the build if line coverage drops below the threshold. Generated
      # files are excluded so the percentage reflects hand-written code only.
      # Current baseline is ~59%; raise `min_coverage` as coverage improves.
      - name: Enforce coverage threshold
        uses: VeryGoodOpenSource/very_good_coverage@v3
        with:
          path: coverage/lcov.info
          min_coverage: 58
          exclude: >-
            **/*.g.dart
            **/*.freezed.dart
            **/*.config.dart
            **/*.gen.dart
            **/firebase_options.dart
            lib/l10n/**
